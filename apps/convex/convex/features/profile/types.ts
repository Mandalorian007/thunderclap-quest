// Profile feature template and action ID enums
// Defines all template and action IDs to prevent circular imports

export enum ProfileTemplateId {
  PROFILE_DISPLAY = "PROFILE_DISPLAY"
}

export enum ProfileActionId {
  // Profile has no actions - it's a terminal template
}