// Progression system template/action IDs
// This file prevents circular imports between functions.ts and templates.ts

export enum ProgressionTemplateId {
  // No templates needed for progression - it's a backend-only feature
}

export enum ProgressionActionId {
  // No actions needed for progression - it's a backend-only feature
}